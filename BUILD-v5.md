# Miles & Meals v5

Complete source release.

## Fixed

- Added Better Auth dynamic `baseURL.fallback`.
- Direct `auth.api.*` calls no longer fail because no request host is available.
- `.env.example` now uses plain environment variable syntax only.
- Resend test sender example uses `onboarding@resend.dev`.
- Login page remains free of the "One active device per account" text.
- Forgot/Reset Password remains included.
- Neon reset script remains included.
